# ❗️ (exclamation point) means non-nullable AKA: REQUIRED
type Coffee {
    id: ID!  # ID (unique identifier) - non-nullable (required)
    name: String!
    brand: String!
    flavors: [String!]!
}

type Query {
    coffees: [Coffee!]!
}
